[Visit](https://anicoderandy.github.io)

# Build
```bash
# Install dependencies
# root permission may be required
gem install bundler jekyll
bundle install

# Test locally
bundle exec jekyll serve --livereload

# Build static files
bundle exec jekyll build
```
